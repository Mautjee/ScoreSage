{
  "name": "@se-2/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/score-sage",
    "create-local": "graph create --node http://localhost:8020/ scaffold-eth/score-sage",
    "remove-local": "graph remove --node http://localhost:8020/ scaffold-eth/score-sage",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 scaffold-eth/score-sage"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.2",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
